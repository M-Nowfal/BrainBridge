import { Request, Response, NextFunction } from "express";

// POST   /api/course
export const createCourse = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// PATCH  /api/course/:id
export const updateCourse = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// DELETE /api/course/:id
export const deleteCourse = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// GET    /api/course/:id
export const getCourse = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// GET    /api/course
export const getCourses = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// POST   /api/course/:id/enroll
export const enrollStudent = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// POST   /api/course/:id/resource
export const addResource = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// POST   /api/course/:id/review
export const postReview = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// GET   /api/course/:id/progress
export const getCourseProgress = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// PATCH  /api/course/:id/publish
export const publishCourse = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};
