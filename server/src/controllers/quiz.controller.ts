import { Request, Response, NextFunction } from "express";

// POST   /api/quiz
export const createQuiz = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// PATCH  /api/quiz/:id
export const updateQuiz = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// DELETE /api/quiz/:id
export const deleteQuiz = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// GET    /api/quiz/:id
export const getQuiz = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// POST   /api/quiz/:id/attempt
export const submitAttempt = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// GET    /api/quiz/:id/results
export const getQuizResults = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};
