import { Request, Response, NextFunction } from "express";

// POST   /api/resource
export const uploadResource = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// PATCH  /api/resource/:id
export const updateResource = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// DELETE /api/resource/:id
export const deleteResource = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// GET    /api/resource/:id
export const getResource = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};

// GET    /api/course/:courseid/resource
export const getCourseResources = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (err: unknown) {
    next(err);
  }
};
